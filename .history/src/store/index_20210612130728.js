import Vue from 'vue'
import Vuex from 'vuex'
import * as fb from '../firebase'
import router from '../'
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
